apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pod3.fullname" . }}-config
  namespace: {{ .Release.namespace }}
data:
{{- range $key, $value := .Values.data }}
{{- $key | nindent 2 }}: {{ $value | quote }}
{{- end }}